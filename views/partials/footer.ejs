<script>
    window.onload = () => {
      if (new URL(location.href).searchParams.get('error')) {
        const msg= new URL(location.href).searchParams.get('error');
        alert(msg);
      }
      if (new URL(location.href).searchParams.get('message')) {
        const msg= new URL(location.href).searchParams.get('message');
        alert(msg);
      }
    };
  </script>
    </body>
    <footer class="footer">
    <p class="col-md-12">
      <span>DIDI.</span>
      <a target="_blank" >Icons by <a href="https://icons8.kr/">Icons8</a></a>
    </p>
    </footer>
</html>